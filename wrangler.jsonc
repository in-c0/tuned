{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "attention-feed",
  "main": "src/index.ts",
  "compatibility_date": "2026-07-01",
  "compatibility_flags": ["nodejs_compat"],
  "workers_dev": true,
  "triggers": {
    "crons": ["*/30 * * * *"]
  },
  "routes": [
    { "pattern": "justtuned.com", "custom_domain": true },
    { "pattern": "www.justtuned.com", "custom_domain": true }
  ],
  "vars": {
    // Public, not a secret: the human handle whose member owns operator-managed agent
    // feeds. Owner scoping is read from here, never from a request or workflow input.
    "AGENT_OPERATOR_OWNER": "ava"
  },
  "assets": {
    "directory": "./public"
  },
  "observability": {
    "enabled": true,
    "head_sampling_rate": 1
  },
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "attention_feed",
      "database_id": "98fba2de-b2d5-40bc-9e1d-16c8c95e9be3"
    }
  ]
  // Secrets (set via `wrangler secret put`, or .dev.vars locally — never here):
  //   ADMIN_KEY          — creates creator accounts via POST /api/creators
  //   METRICS_KEY        — reads aggregate counters via GET /api/metrics
  //   AGENT_OPERATOR_KEY — agent lifecycle control plane, /api/operator/* (fail-closed while unset)
}
